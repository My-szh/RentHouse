<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.RoomFacilityMapper">

    <select id="selectRoomFacilityList" resultType="com.atguigu.lease.model.entity.FacilityInfo">
        select
            rf.facility_id,
            fi.id,
            fi.type,
            fi.name,
            fi.icon
        from room_facility rf
        left join facility_info fi on rf.facility_id = fi.id and fi.type=#{itemType} and fi.is_deleted = 0
        where rf.room_id = #{id} and rf.is_deleted = 0
    </select>
</mapper>
